namespace = oldcountry

country_event = {
	id = oldcountry.1
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		set_country_flag = old_nation
		give_tier_1_techs_effect = yes
		give_tier_2_techs_effect = yes
		add_resource = {
			energy = 1000
			minerals = 1000
			food = 2000
			consumer_goods = 2000
			alloys = 100
			volatile_motes = 200
			rare_crystal = 200
			exotic_gases = 200
			nanites = 50
		}
		add_modifier = { modifier = old_country_1 days = 1800 }
		country_event = {
			id = oldcountry.2
			days = 1800
		}
	}
}

country_event = {
	id = oldcountry.2
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		remove_modifier = old_country_1
		add_modifier = { modifier = old_country_2 days = 3600 }
		country_event = {
			id = oldcountry.3
			days = 3600
		}
	}
}

country_event = {
	id = oldcountry.3
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		remove_modifier = old_country_2
		add_modifier = { modifier = old_country_3 }
		country_event = {
			id = oldcountry.4
			days = 5400
			random = 1800
		}
	}
}

country_event = {
	id = oldcountry.4
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		remove_modifier = old_country_3
		add_modifier = { modifier = old_country_4 }
		country_event = {
			id = oldcountry.5
			days = 5400
			random = 1800
		}
	}
}

country_event = {
	id = oldcountry.5
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		remove_modifier = old_country_4
		country_event = {
			id = oldcountry.6
			days = 1800
			random = 1800
		}
	}
}

country_event = {
	id = oldcountry.6
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		add_modifier = { modifier = old_country_new_ideas }
	}
}